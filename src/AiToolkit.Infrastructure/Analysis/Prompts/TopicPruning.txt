ROLE: The pending search-topic queue has grown too large. Pick the {KeepCount} BEST queries to keep. Optimise for DOMAIN DIVERSITY and CONCRETE WEBSITE-IDEA POTENTIAL — not just monetization.

PERFORMANCE DATA (use as a hint, not as a mandate to clone):
{TopicPerformance}

RANKING RULES:
- DIVERSITY FIRST. Among the queries you keep, no two may target the same narrow vertical. Spread across as many domain families as possible.
- HUMAN-LIFE BIAS. AT LEAST 70% of kept queries must target mass-human life (parenting, money, health, dating, sleep, food, local life, pets, faith, grief, pop-culture fandoms, weddings, travel realities, identity, broad hobbies). AT MOST 30% may target dev/maker/designer/archive/visualization niches.
- Prefer queries that suggest a CONCRETE site idea. Vague queries ("trends 2026", "best tools") are weak.
- HARD-CAP: at most 1 kept query about LLM/AI/SaaS pricing or model routing. At most 1 about shaders / generative art / git tools / cultural-archive remix / browser extensions / color tools — these have already been over-mined.
- Demote: technical CVE / library-internals queries, security forensics, generic listicle bait, and any duplicate-intent pairs (keep the cleanest variant).
- Date qualifiers (2025/2026) help only when freshness matters.

PENDING QUERIES (verbatim — return queries from this exact list):
{TopicList}

OUTPUT — STRICT JSON ARRAY ONLY of exactly {KeepCount} query strings, copied verbatim from the list:
["best query 1", "best query 2", ...]
