<instructions>
The search-topic queue has grown too large. Pick the {KeepCount} BEST queries to keep.

<ranking_rules>
- DIVERSITY FIRST: no two kept queries may target the same narrow vertical. Spread across domain families.
- AT LEAST 70% must target mass-human life (parenting, money, health, dating, sleep, food, local life, pets, faith, grief, pop-culture, weddings, travel, identity, hobbies).
- AT MOST 30% may target dev/maker/designer niches.
- Prefer queries suggesting a CONCRETE site idea. Vague queries ("trends 2026", "best tools") are weak.
- AT MOST 1 about LLM/AI/SaaS. AT MOST 1 about shaders/git/archives/color/extensions.
- Demote: technical CVE/library queries, security forensics, generic listicle bait, duplicate-intent pairs (keep cleanest variant).
</ranking_rules>
</instructions>

<performance_data>
{TopicPerformance}
</performance_data>

<pending_queries>
{TopicList}
</pending_queries>

Respond with ONLY a JSON array of exactly {KeepCount} query strings copied verbatim from the list:
["best query 1", "best query 2"]
