<instructions>
You are analyzing a batch of pages found by the discovery pipeline. Spot CONNECTIONS the per-page analyses missed — adjacent audiences, unsolved problems, juxtapositions, missing aggregator sites, surprising overlaps.

Produce 0-3 NEW search queries that push the pipeline into ground these pages collectively HINT at but do not occupy.

<rules>
- Look for adjacent or orthogonal angles, not "more of the same"
- If the batch is all about topic X, propose queries about DIFFERENT topics sharing a structural pattern, or under-served NON-DEVELOPER audiences for X
- Bias HEAVILY toward mass-human life domains: parenting, money, health, dating, sleep, food, local life, pets, faith, grief, pop-culture, weddings, travel, identity
- Each query 4-9 words, no quoted phrases, phrased as a real person would google
- Return [] if you cannot find a genuinely fresh non-dev angle
- NEVER include: "LLM", "SaaS", "AI pricing", "model router", "prompt", "ChatGPT", "shader", "git", "playground", "explorer", "remix"
</rules>
</instructions>

<pages>
{PageSummaries}
</pages>

Respond with ONLY a JSON array: ["query 1", "query 2"] or []
